springboot:
  application:
    name: Aggregation-Service
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:oracle://localhost/test

spring.boot: this is Aggregation service config for Default
server:
  port: 9010

