springboot:
  application:
    name: Login-Service
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:oracle://localhost/test

spring.boot: this is Login Service config for Production Enviroment
server:
  port: 9010
