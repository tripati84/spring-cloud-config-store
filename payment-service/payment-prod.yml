springboot:
  application:
    name: Payment-Service
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:oracle://localhost/test

spring.boot: this is Payment Service config for Production Enviroment
server:
  port: 9010
